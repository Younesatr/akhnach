When project structure or features are updated:
- Check if README.md needs corresponding updates
- Update file structure section if new files are added
- Update features list if new functionality is added
- Update customization instructions if new editable sections are created
- Update color scheme if design system changes
- Keep setup instructions current with any deployment changes
